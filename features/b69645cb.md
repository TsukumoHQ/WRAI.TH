# b69645cb

## Team : wraith-engine-2 (tsukumo)
## Branch : wraith-engine-2/watchdog-doc-fix (from main)
## Status : 🔵 SUBMITTED

## 1. Product Brief

### Acceptance Criteria
_(untyped ticket — no acceptance criteria)_

## 2. Root cause & decisions

# watchdog stuck-agent doc/test fix (follow-up to 05fc4373 / review finding)

ROOT_CAUSE: review-05fc4373 (major, non-blocking) found StuckAgents' doc claimed gracefully-deactivated agents are EXCLUDED, but the query is `status IN ('active','inactive')` and DeactivateAgent sets status='inactive' (agents.go) — so they are INCLUDED. TestStuckAgents_ExcludesDeactivated asserted on status='deactivated', a value NO code ever sets, so it validated nothing (a doc/test lie). Functionally benign (a gracefully-down agent still holding work is equally a requeue candidate) but the claim was false.

## Decision
Make the doc + test truthful to the actual, correct behavior (no behavior change):
- watchdog.go doc now states 'active' AND 'inactive' are both candidates (a hung agent holding a live lease stays 'active' — MarkStaleAgentsInactive protects a live-lease holder; a long-hung one is swept to 'inactive'; graceful DeactivateAgent also sets 'inactive' and is indistinguishable), and only 'sleeping' (parked) + 'deleted' (tombstoned) are excluded.
- Replaced the false TestStuckAgents_ExcludesDeactivated with TestStuckAgents_ExcludesParkedAndDeleted (asserts the REAL excluded statuses) + added TestStuckAgents_IncludesInactive (locks the documented scope so doc/test can't drift again).

## Gate
build -tags fts5 OK / vet OK / gofmt OK / test -tags fts5 OK (db+relay 460 pass). Doc + test only; no logic/schema change.

## 3. Files changed

```
internal/db/watchdog.go      | 15 ++++++++++++---
 internal/db/watchdog_test.go | 44 ++++++++++++++++++++++++++++++++++++--------
 2 files changed, 48 insertions(+), 11 deletions(-)
```

## 4. QA Log

_(no review round yet)_

## 5. Timeline


---
_Auto-assembled by the niwa scribe from the Q&A gate. Task `b69645cb`._
