# 1ec6383d

## Team : wraith-engine-2 (tsukumo)
## Branch : wraith-engine-2/cron-durable-test (from main)
## Status : 🔵 SUBMITTED

## 1. Product Brief

### Acceptance Criteria
_(untyped ticket — no acceptance criteria)_

## 2. Root cause & decisions

# cron dedup skip-branch test (follow-up to 553f17ce / review nit)

ROOT_CAUSE: review-553f17ce (notice, non-blocking) — the read-back skip branch in fireCronSchedule (the guard that prevents a duplicate ticket when the dedup marker write silently fails) had no test, so a regression dropping the guard would pass CI.

## Decision
Extract the marker write+read-back into `persistFireMarker(key, val) bool` and test both outcomes: a normal write confirms durable (true); a failed write (injected by closing the DB — SetSetting swallows the error, GetSetting reads nothing back) reports false so the fire is skipped. Behavior unchanged; the guard is now regression-covered without adding a production fault-injection seam.

## Gate
build -tags fts5 OK / vet OK / gofmt OK / test -tags fts5 OK — 531 pass. Trivial extraction + one test. No schema.

## 3. Files changed

```
internal/relay/cron.go      | 14 +++++++++++---
 internal/relay/cron_test.go | 18 ++++++++++++++++++
 2 files changed, 29 insertions(+), 3 deletions(-)
```

## 4. QA Log

_(no review round yet)_

## 5. Timeline


---
_Auto-assembled by the niwa scribe from the Q&A gate. Task `1ec6383d`._
